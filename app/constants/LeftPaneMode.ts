// TypeScript enum for LeftPaneMode
export enum LeftPaneMode {
    INBOX_ARCHIVE = 'INBOX_ARCHIVE',
    MESSAGE_REQUESTS = 'MESSAGE_REQUESTS',
    FILTER = 'FILTER',
    // add other modes here as needed
  }
  